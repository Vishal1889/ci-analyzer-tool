# CLI-only requirements — used by Docker image (no GUI needed)
# For GUI/executable builds, use requirements.txt instead
requests>=2.31.0
requests-oauthlib>=1.3.1
python-dotenv>=1.0.0
pandas>=2.2.0
cron-descriptor>=1.4.0
cryptography>=41.0.0